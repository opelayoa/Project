package com.tiendas3b.almacen.db.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.tiendas3b.almacen.db.dao.Roadmap;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ROADMAP".
*/
public class RoadmapDao extends AbstractDao<Roadmap, Long> {

    public static final String TABLENAME = "ROADMAP";

    /**
     * Properties of entity Roadmap.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Sync = new Property(1, boolean.class, "sync", false, "SYNC");
        public final static Property Scaffolds = new Property(2, Integer.class, "scaffolds", false, "SCAFFOLDS");
        public final static Property UserId = new Property(3, long.class, "userId", false, "USER_ID");
        public final static Property RegionId = new Property(4, long.class, "regionId", false, "REGION_ID");
        public final static Property DriverId = new Property(5, long.class, "driverId", false, "DRIVER_ID");
        public final static Property TruckId = new Property(6, long.class, "truckId", false, "TRUCK_ID");
        public final static Property Travel = new Property(7, int.class, "travel", false, "TRAVEL");
        public final static Property Date = new Property(8, String.class, "date", false, "DATE");
    };

    private DaoSession daoSession;


    public RoadmapDao(DaoConfig config) {
        super(config);
    }
    
    public RoadmapDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ROADMAP\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"SYNC\" INTEGER NOT NULL ," + // 1: sync
                "\"SCAFFOLDS\" INTEGER," + // 2: scaffolds
                "\"USER_ID\" INTEGER NOT NULL ," + // 3: userId
                "\"REGION_ID\" INTEGER NOT NULL ," + // 4: regionId
                "\"DRIVER_ID\" INTEGER NOT NULL ," + // 5: driverId
                "\"TRUCK_ID\" INTEGER NOT NULL ," + // 6: truckId
                "\"TRAVEL\" INTEGER NOT NULL ," + // 7: travel
                "\"DATE\" TEXT NOT NULL );"); // 8: date
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_ROADMAP_REGION_ID_DRIVER_ID_TRUCK_ID_TRAVEL_DATE ON ROADMAP" +
                " (\"REGION_ID\",\"DRIVER_ID\",\"TRUCK_ID\",\"TRAVEL\",\"DATE\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ROADMAP\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Roadmap entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getSync() ? 1L: 0L);
 
        Integer scaffolds = entity.getScaffolds();
        if (scaffolds != null) {
            stmt.bindLong(3, scaffolds);
        }
        stmt.bindLong(4, entity.getUserId());
        stmt.bindLong(5, entity.getRegionId());
        stmt.bindLong(6, entity.getDriverId());
        stmt.bindLong(7, entity.getTruckId());
        stmt.bindLong(8, entity.getTravel());
        stmt.bindString(9, entity.getDate());
    }

    @Override
    protected void attachEntity(Roadmap entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Roadmap readEntity(Cursor cursor, int offset) {
        Roadmap entity = new Roadmap( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getShort(offset + 1) != 0, // sync
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // scaffolds
            cursor.getLong(offset + 3), // userId
            cursor.getLong(offset + 4), // regionId
            cursor.getLong(offset + 5), // driverId
            cursor.getLong(offset + 6), // truckId
            cursor.getInt(offset + 7), // travel
            cursor.getString(offset + 8) // date
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Roadmap entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setSync(cursor.getShort(offset + 1) != 0);
        entity.setScaffolds(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setUserId(cursor.getLong(offset + 3));
        entity.setRegionId(cursor.getLong(offset + 4));
        entity.setDriverId(cursor.getLong(offset + 5));
        entity.setTruckId(cursor.getLong(offset + 6));
        entity.setTravel(cursor.getInt(offset + 7));
        entity.setDate(cursor.getString(offset + 8));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Roadmap entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Roadmap entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getRegionDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getDriverDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T2", daoSession.getTruckDao().getAllColumns());
            builder.append(" FROM ROADMAP T");
            builder.append(" LEFT JOIN REGION T0 ON T.\"REGION_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN DRIVER T1 ON T.\"DRIVER_ID\"=T1.\"_id\"");
            builder.append(" LEFT JOIN TRUCK T2 ON T.\"TRUCK_ID\"=T2.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Roadmap loadCurrentDeep(Cursor cursor, boolean lock) {
        Roadmap entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Region region = loadCurrentOther(daoSession.getRegionDao(), cursor, offset);
         if(region != null) {
            entity.setRegion(region);
        }
        offset += daoSession.getRegionDao().getAllColumns().length;

        Driver driver = loadCurrentOther(daoSession.getDriverDao(), cursor, offset);
         if(driver != null) {
            entity.setDriver(driver);
        }
        offset += daoSession.getDriverDao().getAllColumns().length;

        Truck truck = loadCurrentOther(daoSession.getTruckDao(), cursor, offset);
         if(truck != null) {
            entity.setTruck(truck);
        }

        return entity;    
    }

    public Roadmap loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Roadmap> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Roadmap> list = new ArrayList<Roadmap>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Roadmap> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Roadmap> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
