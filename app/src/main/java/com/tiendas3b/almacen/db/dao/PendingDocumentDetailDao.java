package com.tiendas3b.almacen.db.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import com.tiendas3b.almacen.db.dao.PendingDocumentDetail;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PENDING_DOCUMENT_DETAIL".
*/
public class PendingDocumentDetailDao extends AbstractDao<PendingDocumentDetail, Long> {

    public static final String TABLENAME = "PENDING_DOCUMENT_DETAIL";

    /**
     * Properties of entity PendingDocumentDetail.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Amount = new Property(1, Integer.class, "amount", false, "AMOUNT");
        public final static Property Stores = new Property(2, Integer.class, "stores", false, "STORES");
        public final static Property Events = new Property(3, Integer.class, "events", false, "EVENTS");
        public final static Property Type = new Property(4, String.class, "type", false, "TYPE");
        public final static Property PendingDocumentId = new Property(5, long.class, "pendingDocumentId", false, "PENDING_DOCUMENT_ID");
    };

    private DaoSession daoSession;


    public PendingDocumentDetailDao(DaoConfig config) {
        super(config);
    }
    
    public PendingDocumentDetailDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PENDING_DOCUMENT_DETAIL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"AMOUNT\" INTEGER," + // 1: amount
                "\"STORES\" INTEGER," + // 2: stores
                "\"EVENTS\" INTEGER," + // 3: events
                "\"TYPE\" TEXT NOT NULL ," + // 4: type
                "\"PENDING_DOCUMENT_ID\" INTEGER NOT NULL );"); // 5: pendingDocumentId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_PENDING_DOCUMENT_DETAIL_PENDING_DOCUMENT_ID_TYPE ON PENDING_DOCUMENT_DETAIL" +
                " (\"PENDING_DOCUMENT_ID\",\"TYPE\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PENDING_DOCUMENT_DETAIL\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, PendingDocumentDetail entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Integer amount = entity.getAmount();
        if (amount != null) {
            stmt.bindLong(2, amount);
        }
 
        Integer stores = entity.getStores();
        if (stores != null) {
            stmt.bindLong(3, stores);
        }
 
        Integer events = entity.getEvents();
        if (events != null) {
            stmt.bindLong(4, events);
        }
        stmt.bindString(5, entity.getType());
        stmt.bindLong(6, entity.getPendingDocumentId());
    }

    @Override
    protected void attachEntity(PendingDocumentDetail entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public PendingDocumentDetail readEntity(Cursor cursor, int offset) {
        PendingDocumentDetail entity = new PendingDocumentDetail( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // amount
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // stores
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3), // events
            cursor.getString(offset + 4), // type
            cursor.getLong(offset + 5) // pendingDocumentId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, PendingDocumentDetail entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAmount(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setStores(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setEvents(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
        entity.setType(cursor.getString(offset + 4));
        entity.setPendingDocumentId(cursor.getLong(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(PendingDocumentDetail entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(PendingDocumentDetail entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPendingDocumentDao().getAllColumns());
            builder.append(" FROM PENDING_DOCUMENT_DETAIL T");
            builder.append(" LEFT JOIN PENDING_DOCUMENT T0 ON T.\"PENDING_DOCUMENT_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PendingDocumentDetail loadCurrentDeep(Cursor cursor, boolean lock) {
        PendingDocumentDetail entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        PendingDocument pendingDocument = loadCurrentOther(daoSession.getPendingDocumentDao(), cursor, offset);
         if(pendingDocument != null) {
            entity.setPendingDocument(pendingDocument);
        }

        return entity;    
    }

    public PendingDocumentDetail loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PendingDocumentDetail> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PendingDocumentDetail> list = new ArrayList<PendingDocumentDetail>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PendingDocumentDetail> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PendingDocumentDetail> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
