package com.tiendas3b.almacen.db.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import com.tiendas3b.almacen.db.dao.OrderDetailCapture;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ORDER_DETAIL_CAPTURE".
*/
public class OrderDetailCaptureDao extends AbstractDao<OrderDetailCapture, Long> {

    public static final String TABLENAME = "ORDER_DETAIL_CAPTURE";

    /**
     * Properties of entity OrderDetailCapture.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Order = new Property(1, int.class, "order", false, "ORDER");
        public final static Property Quantity = new Property(2, int.class, "quantity", false, "QUANTITY");
        public final static Property Type = new Property(3, String.class, "type", false, "TYPE");
        public final static Property Status = new Property(4, String.class, "status", false, "STATUS");
        public final static Property Packing = new Property(5, int.class, "packing", false, "PACKING");
        public final static Property FatherIclave = new Property(6, Integer.class, "fatherIclave", false, "FATHER_ICLAVE");
        public final static Property Child = new Property(7, boolean.class, "child", false, "CHILD");
        public final static Property OrderCaptureId = new Property(8, long.class, "orderCaptureId", false, "ORDER_CAPTURE_ID");
        public final static Property ArticleId = new Property(9, long.class, "articleId", false, "ARTICLE_ID");
        public final static Property Iclave = new Property(10, int.class, "iclave", false, "ICLAVE");
    };

    private DaoSession daoSession;

    private Query<OrderDetailCapture> orderPickingCapture_DetailsQuery;

    public OrderDetailCaptureDao(DaoConfig config) {
        super(config);
    }
    
    public OrderDetailCaptureDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ORDER_DETAIL_CAPTURE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"ORDER\" INTEGER NOT NULL ," + // 1: order
                "\"QUANTITY\" INTEGER NOT NULL ," + // 2: quantity
                "\"TYPE\" TEXT NOT NULL ," + // 3: type
                "\"STATUS\" TEXT NOT NULL ," + // 4: status
                "\"PACKING\" INTEGER NOT NULL ," + // 5: packing
                "\"FATHER_ICLAVE\" INTEGER," + // 6: fatherIclave
                "\"CHILD\" INTEGER NOT NULL ," + // 7: child
                "\"ORDER_CAPTURE_ID\" INTEGER NOT NULL ," + // 8: orderCaptureId
                "\"ARTICLE_ID\" INTEGER NOT NULL ," + // 9: articleId
                "\"ICLAVE\" INTEGER NOT NULL );"); // 10: iclave
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_ORDER_DETAIL_CAPTURE_ORDER_CAPTURE_ID_ICLAVE ON ORDER_DETAIL_CAPTURE" +
                " (\"ORDER_CAPTURE_ID\",\"ICLAVE\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ORDER_DETAIL_CAPTURE\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, OrderDetailCapture entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getOrder());
        stmt.bindLong(3, entity.getQuantity());
        stmt.bindString(4, entity.getType());
        stmt.bindString(5, entity.getStatus());
        stmt.bindLong(6, entity.getPacking());
 
        Integer fatherIclave = entity.getFatherIclave();
        if (fatherIclave != null) {
            stmt.bindLong(7, fatherIclave);
        }
        stmt.bindLong(8, entity.getChild() ? 1L: 0L);
        stmt.bindLong(9, entity.getOrderCaptureId());
        stmt.bindLong(10, entity.getArticleId());
        stmt.bindLong(11, entity.getIclave());
    }

    @Override
    protected void attachEntity(OrderDetailCapture entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public OrderDetailCapture readEntity(Cursor cursor, int offset) {
        OrderDetailCapture entity = new OrderDetailCapture( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getInt(offset + 1), // order
            cursor.getInt(offset + 2), // quantity
            cursor.getString(offset + 3), // type
            cursor.getString(offset + 4), // status
            cursor.getInt(offset + 5), // packing
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // fatherIclave
            cursor.getShort(offset + 7) != 0, // child
            cursor.getLong(offset + 8), // orderCaptureId
            cursor.getLong(offset + 9), // articleId
            cursor.getInt(offset + 10) // iclave
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, OrderDetailCapture entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setOrder(cursor.getInt(offset + 1));
        entity.setQuantity(cursor.getInt(offset + 2));
        entity.setType(cursor.getString(offset + 3));
        entity.setStatus(cursor.getString(offset + 4));
        entity.setPacking(cursor.getInt(offset + 5));
        entity.setFatherIclave(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setChild(cursor.getShort(offset + 7) != 0);
        entity.setOrderCaptureId(cursor.getLong(offset + 8));
        entity.setArticleId(cursor.getLong(offset + 9));
        entity.setIclave(cursor.getInt(offset + 10));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(OrderDetailCapture entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(OrderDetailCapture entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "details" to-many relationship of OrderPickingCapture. */
    public List<OrderDetailCapture> _queryOrderPickingCapture_Details(long orderCaptureId) {
        synchronized (this) {
            if (orderPickingCapture_DetailsQuery == null) {
                QueryBuilder<OrderDetailCapture> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.OrderCaptureId.eq(null));
                orderPickingCapture_DetailsQuery = queryBuilder.build();
            }
        }
        Query<OrderDetailCapture> query = orderPickingCapture_DetailsQuery.forCurrentThread();
        query.setParameter(0, orderCaptureId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getOrderPickingCaptureDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getVArticleDao().getAllColumns());
            builder.append(" FROM ORDER_DETAIL_CAPTURE T");
            builder.append(" LEFT JOIN ORDER_PICKING_CAPTURE T0 ON T.\"ORDER_CAPTURE_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN VARTICLE T1 ON T.\"ARTICLE_ID\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected OrderDetailCapture loadCurrentDeep(Cursor cursor, boolean lock) {
        OrderDetailCapture entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        OrderPickingCapture orderCapture = loadCurrentOther(daoSession.getOrderPickingCaptureDao(), cursor, offset);
         if(orderCapture != null) {
            entity.setOrderCapture(orderCapture);
        }
        offset += daoSession.getOrderPickingCaptureDao().getAllColumns().length;

        VArticle article = loadCurrentOther(daoSession.getVArticleDao(), cursor, offset);
         if(article != null) {
            entity.setArticle(article);
        }

        return entity;    
    }

    public OrderDetailCapture loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<OrderDetailCapture> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<OrderDetailCapture> list = new ArrayList<OrderDetailCapture>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<OrderDetailCapture> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<OrderDetailCapture> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
