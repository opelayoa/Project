package com.tiendas3b.almacen.db.dao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.tiendas3b.almacen.db.dao.Driver;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DRIVER".
*/
public class DriverDao extends AbstractDao<Driver, Long> {

    public static final String TABLENAME = "DRIVER";

    /**
     * Properties of entity Driver.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Description = new Property(1, String.class, "description", false, "DESCRIPTION");
        public final static Property Active = new Property(2, Integer.class, "active", false, "ACTIVE");
        public final static Property RegionId = new Property(3, long.class, "regionId", false, "REGION_ID");
        public final static Property DriverId = new Property(4, long.class, "driverId", false, "DRIVER_ID");
    };


    public DriverDao(DaoConfig config) {
        super(config);
    }
    
    public DriverDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DRIVER\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"DESCRIPTION\" TEXT," + // 1: description
                "\"ACTIVE\" INTEGER," + // 2: active
                "\"REGION_ID\" INTEGER NOT NULL ," + // 3: regionId
                "\"DRIVER_ID\" INTEGER NOT NULL );"); // 4: driverId
        // Add Indexes
        db.execSQL("CREATE UNIQUE INDEX " + constraint + "IDX_DRIVER_REGION_ID_DRIVER_ID ON DRIVER" +
                " (\"REGION_ID\",\"DRIVER_ID\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DRIVER\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, Driver entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String description = entity.getDescription();
        if (description != null) {
            stmt.bindString(2, description);
        }
 
        Integer active = entity.getActive();
        if (active != null) {
            stmt.bindLong(3, active);
        }
        stmt.bindLong(4, entity.getRegionId());
        stmt.bindLong(5, entity.getDriverId());
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public Driver readEntity(Cursor cursor, int offset) {
        Driver entity = new Driver( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // description
            cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2), // active
            cursor.getLong(offset + 3), // regionId
            cursor.getLong(offset + 4) // driverId
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, Driver entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDescription(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setActive(cursor.isNull(offset + 2) ? null : cursor.getInt(offset + 2));
        entity.setRegionId(cursor.getLong(offset + 3));
        entity.setDriverId(cursor.getLong(offset + 4));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(Driver entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(Driver entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
